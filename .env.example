# Example environment variables for Smart JISA

# Your Google Gemini API Key (do not commit actual key in .env)
GOOGLE_API_KEY=your_api_key_here

# Application metadata (optional, can be default values)
APP_NAME=smart_jisa_app
DEFAULT_USER_ID=default_user
DEFAULT_SESSION_ID=default_session
